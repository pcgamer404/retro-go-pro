PATCH INSTRUCTIONS for components/SDL/SDL_system.c
====================================================

Find the SDL_InitSD() function (starts around line 107) and replace its
ENTIRE body with a no-op. retro-go's rg_storage_init() (called as part
of rg_system_init(), before app_main's cgTask runs) already mounts /sd
-- this function doing it again both fails to compile against IDF
5.3.5's newer sdspi API (sdspi_slot_config_t/HSPI_HOST no longer exist)
and would conflict with the SPI bus retro-go already owns anyway (see
the "SPI bus already initialized" warning that shows up in every other
port's boot log for the same underlying reason).

Change:

void SDL_InitSD(void)
{
    printf("Initialising SD Card\n");
#if 1
	sdmmc_host_t host = SDSPI_HOST_DEFAULT();
    ... (rest of the function) ...
}

PATCH INSTRUCTIONS (add this too):

At the top of SDL_system.c, add:

#include <esp_timer.h>

(needed for esp_timer_get_time() used in SDL_GetTicks() -- "implicit
declaration" error otherwise on this toolchain.)

